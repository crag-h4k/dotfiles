[core]
    excludesfile = ~/.gitignore_global
    editor = nvim
[diff]
    tool = nvimdiff
[init]
    defaultBranch = main

[pull]
    rebase = false

[push]
    autoSetupRemote = true
    default = current

[alias]
    showtrackedignored = ls-files -i --exclude-standard

[color]
    branch = auto
    diff = auto
    interactive = auto
    status = auto

[user]
    useConfigOnly = true

# Personal identity by default. Fill in ~/.gitconfig.personal on each host.
[include]
    path = ~/.gitconfig.personal

# Per-context override. Uncomment and adapt as needed.
# [includeIf "gitdir:~/work/**"]
#     path = ~/.gitconfig.work

# GitHub CLI credential helper. PATH-relative so it works on macOS (Homebrew),
# Intel Macs, and Debian alike - wherever `gh` resolves on PATH.
[credential "https://github.com"]
	helper =
	helper = !gh auth git-credential
[credential "https://gist.github.com"]
	helper =
	helper = !gh auth git-credential
